# Task: Update Project README to Match Current Python Codebase

1. Analyze the full repository structure to understand current modules, entry points, and architecture.  
2. Identify how the application is executed, including CLI commands, scripts, or main modules.  
3. Extract all external dependencies from `requirements.txt`, `pyproject.toml`, or equivalent configuration files.  
4. Inspect key public APIs, services, or core functions exposed by the codebase.  
5. Update the installation section to reflect the exact current dependency and setup process.  
6. Rewrite usage examples to match real execution flows and actual code behavior.  
7. Document all required environment variables and configuration options based on the code.  
8. Update the project structure section to accurately reflect the current directory layout.  
9. Remove any outdated, deprecated, or unused instructions, features, or references.  
10. Ensure all code snippets in the README are runnable and consistent with the latest implementation.  
11. Validate clarity, consistency, and developer usability of the final README.  


Do not overuse emojis, and keep the readme concise and to the point.
Do not include sections like "LICENSE", "CONTRIBUTING", "CHANGELOG", etc. There are dedicated files for those sections.
Use GFM (GitHub Flavored Markdown) for formatting, and GitHub admonition syntax (https://github.com/orgs/community/discussions/16925) where appropriate.
If you find a logo or icon for the project, use it in the readme's header.