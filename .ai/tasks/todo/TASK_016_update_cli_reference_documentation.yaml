# Phase 01 Validation Sources: CLI-003 (HIGH, mandatory)
# Rewrites cli-reference.md for vkdownloader CLI

id: TASK_016_update_cli_reference_documentation

title: Update CLI reference documentation for vkdownloader

priority: high

source_reference: .ai\audit\99-validation\01-cli-validated-findings.md

source_section: CLI-003

files:
  - path: docs/99-reference/cli-reference.md

changes:
  - action: rewrite
    description: >
      Remove or rewrite docs/99-reference/cli-reference.md.
      Current document references "mko-telebot" commands (init, validate, run, config, version)
      which are completely different from actual "vkdownloader" commands (download, batch).
      Create accurate documentation for vkdownloader CLI.
goals:
  - Document actual vkdownloader download command
  - Document actual vkdownloader batch command
  - Remove mko-telebot misreference
  - Provide accurate CLI usage documentation
acceptance_criteria:
  - cli-reference.md documents vkdownloader commands
  - download and batch command options documented
  - No references to non-existent mko-telebot commands