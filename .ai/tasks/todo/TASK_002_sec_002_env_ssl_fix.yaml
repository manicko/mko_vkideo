id: TASK_002_sec_002_env_ssl_fix
source_reference: .ai/audit/99-validation/04-security-validated.md
source_section: SEC-002
source_finding: SEC-002

title: Remove insecure SSL verify setting from tracked .env
priority: high
status: pending

description: >
  The .env file in the repository sets VKDOWNLOADER_SSL_VERIFY=false, contradicting the documented
  secure default. This creates confusion for users and incorrect defaults for programmatic usage.

goals:
  - Remove or comment the VKDOWNLOADER_SSL_VERIFY=false line from .env
  - Ensure secure default (ssl_verify=True) holds for programmatic users

files:
  - path: .env

changes:
  - action: remove_line
    description: >
      Remove or comment the insecure SSL verify setting.
    code_hint: |
      # VKDOWNLOADER_SSL_VERIFY=false  # Commented out - secure default is True

acceptance_criteria:
  - .env no longer sets insecure SSL default
  - Documented behavior matches actual behavior